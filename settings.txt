# Настройки бота для рассылки

# Задержка в секундах между отправкой сообщений (обязательно больше 10)
time_per_message=15

# Количество циклов для отправки сообщений
messages_per_cycle=5

# Задержка в секундах в цикле за один шаг (обязательно больше 10)
delay_between_messages=30

# Задержка в секундах между переключением сессий (чем меньше чем сессии быстрее меняются)
delay_between_sessions=180
